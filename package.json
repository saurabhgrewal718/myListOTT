{
  "name": "mylistott",
  "version": "1.0.0",
  "description": "This is the Backend Implementation of any OTT app where we are implementing the My List feature.",
  "main": "build/index.js",
  "scripts": {
    "build": "rm -rf build && tsc -p . --outDir build",
    "start": "node build/index.js",
    "test": "mocha --require ts-node/register 'test/**/*.spec.ts'",
    "setup": "npm install && npm run build",
    "dev": "npm run build && npm start"
  },
  "keywords": [],
  "author": "Saurabh Grewal",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "mongodb": "^6.5.0",
    "mongoose": "^8.2.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30",
    "chai": "^5.1.0",
    "chai-http": "^4.4.0",
    "mocha": "^10.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}
